PORT=4000
JWT_SECRET=replace-with-long-random-string
JWT_EXPIRES_IN=2h
BCRYPT_SALT_ROUNDS=12
JSON_BODY_LIMIT=1mb

PGHOST=localhost
PGPORT=5432
PGDATABASE=meteorroids
PGUSER=your_db_user
PGPASSWORD=yash2121
PGSSL=false

# Optional comma-separated list of allowed origins for CORS
# CORS_ORIGIN=http://localhost:3000
